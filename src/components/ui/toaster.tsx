
// This file is no longer used as we've switched to Sonner.
// It's kept for backward compatibility but doesn't render anything.
// All toast functionality should use the Sonner component in App.tsx.

import React from "react";

export function Toaster() {
  // This component is intentionally empty as we're using Sonner instead
  return null;
}
